import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: { main: '#e63946' },
    secondary: { main: '#f1faee' },
    background: { default: '#f8f9fa' },
  },
});

export default theme;
